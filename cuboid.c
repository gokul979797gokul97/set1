#include<stdio.h>
int main()
{
   int 1,b,h,volume,surface;
   scanf("%d %d %d",&1,&b,&h);
   volume =1*b*h;
    surface=(2*1*b)+(2*b*h)+(2*1*h);
    printf("%d %d",surface,volume);
    return 0;
    }
